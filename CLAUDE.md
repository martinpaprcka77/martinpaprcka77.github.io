# CLAUDE.md

> **Canonical AI instructions live in [`AGENTS.md`](AGENTS.md).** This file is a
> deliberate pointer so the two cannot drift. Read `AGENTS.md` first — it covers the
> monorepo (profile/ + toolkit/), the module, tests, conventions, and the prompt index.
