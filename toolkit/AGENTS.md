# AGENTS.md — toolkit/

> **Canonical AI instructions: [`../AGENTS.md`](../AGENTS.md).** That file documents the
> whole monorepo and already includes the toolkit layout
> (`toolkit/Toolkit/{Private,Public,Public/Menu}`, `ops/`, `config/`, `build/`) and the
> module/test conventions. This pointer exists so there is exactly one source of truth —
> do not add instruction content here.
